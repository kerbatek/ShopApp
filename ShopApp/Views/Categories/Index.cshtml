@model IEnumerable<ShopApp.Models.Catalog.Category>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Categories List";
    ViewData["Stylesheet"] = "~/css/tables.css";
}

<div class="container">
    <a asp-action="Create" class="create-button">
        <i class="fas fa-plus"></i> Create New Category
    </a>
    @if (Model.Any())
    {
        <table>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Description</th>
            </tr>
            @foreach (var category in Model)
            {
                <tr>
                    <td>@category.CategoryID</td>
                    <td>@category.CategoryName</td>
                    <td>@category.CategoryDescription</td>
                    <td>@category.CreatedAt?.ToLocalTime()</td>
                    <td>@category.UpdatedAt?.ToLocalTime()</td>
                    <td>
                        <form asp-action="Edit" method="get" class="action-form">
                            <input type="hidden" name="id" value="@category.CategoryID" />
                            @Html.AntiForgeryToken()
                            <button type="submit" class="edit-button">
                                <i class="fas fa-edit"></i>
                            </button>
                        </form>
                    
                        <form asp-action="Delete" method="post" class="action-form" onsubmit="return confirm('Are you sure you want to delete this category?');">
                            <input type="hidden" name="id" value="@category.CategoryID"/>
                            @Html.AntiForgeryToken()
                            <button type="submit" class="delete-button">
                                <i class="fas fa-trash"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </table>
    }
</div>